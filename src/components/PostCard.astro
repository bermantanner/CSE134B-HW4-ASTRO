---
const { title, description, date, slug } = Astro.props;
---

<article style="border: 1px solid #aaa2a2; padding: 1rem; margin: 1rem 0;">
  <h2><a href={`/posts/${slug}/`}>{title}</a></h2>
  <p><small>{date}</small></p>
  <p>{description}</p>
</article>
